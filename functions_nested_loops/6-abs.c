#include "main.h"

/**
 * _abs - Finding absolute value
 *@number: variable
 * Return: The integer w the positive value
 */
int _abs(int number)
{
	if (number < 0)
	{
		number = -number;
	}
	return (number);
}
